function sum(a: number, b: number): number {
    return a + b;
}
sum(10, 20)

// Compile : ts 파일을 js 파일로 변환 하는 작업

// tsc : typescript compile
// tsc index.ts
// index.js 파일 생성
// tsc 명령어 항상 쓸 수 없으니 webpack 이용